CREATE TABLE TPROJECT(	
	PROJECT_CODE VARCHAR(30) PRIMARY KEY,
	PROJECT_NAME VARCHAR(100)
);
GO 
INSERT INTO TPROJECT(PROJECT_CODE, PROJECT_NAME) VALUES('01', 'PUSAT');
INSERT INTO TPROJECT(PROJECT_CODE, PROJECT_NAME) VALUES('11', 'CABANG 1');
INSERT INTO TPROJECT(PROJECT_CODE, PROJECT_NAME) VALUES('12', 'CABANG 2');
GO


CREATE TABLE TWAREHOUSE(
	ID INT PRIMARY KEY IDENTITY(1,1),
	KODE VARCHAR(30) UNIQUE,
	NAMA VARCHAR(100),		
	ISACTIVE SMALLINT,
	JENIS SMALLINT,
	IS_EXTERNAL SMALLINT DEFAULT 0,
	PROJECT_CODE VARCHAR(30)
)

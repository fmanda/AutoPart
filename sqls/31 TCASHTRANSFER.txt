CREATE TABLE TCASHTRANSFER(
	ID INT PRIMARY KEY IDENTITY(1,1),
	REFNO VARCHAR(100),
	TRANSDATE DATE,	
	DUEDATE DATE,	
	REKENING_ID INT FOREIGN KEY REFERENCES TREKENING(ID),
	AMOUNT FLOAT,
	NOTES VARCHAR(300),
	CLOSED SMALLINT,
	MODIFIEDDATE DATETIME2(0),
	MODIFIEDBY VARCHAR(30)
)
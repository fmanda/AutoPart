CREATE TABLE TSERVICEDETAIL(
	ID INT PRIMARY KEY IDENTITY(1,1),
	SALESINVOICE_ID INT FOREIGN KEY REFERENCES TSALESINVOICE(ID),
	SERVICE_ID INT FOREIGN KEY REFERENCES TSERVICE(ID),
	QTY FLOAT,
	HARGA FLOAT,
	DISCOUNT FLOAT,
	PPN FLOAT,
	TOTAL FLOAT
)